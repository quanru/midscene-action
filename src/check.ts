// v0.2: create a GitHub Check Run (conclusion, annotations, details_url
// pointing at the workflow run). Intentionally unused in v0.1 — results are
// reported through the job conclusion, annotations and the PR comment.
export {};
